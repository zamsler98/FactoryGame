Place images, audio, etc. here. Load them in `game_app` using `macroquad::texture::load_texture` or embed them.
Macroquad's loading works on desktop, mobile, and web (with small differences for wasm).
